# Redirects from what the browser requests to what netlify serves
/*.html / 200!

# Redirect default Netlify subdomain to primary domain
https://roblouw.netlify.com/* https://www.roblouw.com/:splat 301!